<%- "<?php" %>

define('POST_THUMBNAILS_MAP', array (
<% if (imageMaps.postThumbnail) { %>
  <% imageMaps.postThumbnail.forEach(function(thumb) { %>
    '<%- thumb.from %>' => '<%- thumb.to %>',
  <% }); %>
<% } %>
));

define('CONTENT_IMAGES_MAP', array (
<% if (imageMaps.contentImage) { %>
  <% imageMaps.contentImage.forEach(function(image) { %>
    '<%- image.from %>' => '<%- image.to %>',
  <% }); %>
<% } %>
));

define('GALLERY_IMAGES_MAP', array (
<% if (imageMaps.galleryImage) { %>
  <% imageMaps.galleryImage.forEach(function(image) { %>
    '<%- image.from %>' => '<%- image.to %>',
  <% }); %>
<% } %>
));

define('GALLERY_THUMBNAIL_MAP', array (
<% if (imageMaps.galleryThumbnail) { %>
  <% imageMaps.galleryThumbnail.forEach(function(image) { %>
    '<%- image.from %>' => '<%- image.to %>',
  <% }); %>
<% } %>
));

